<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= movie.name %> - Detail</title>
</head>
<body>
  <%- include('partials/nav') %>

  <h1><%= movie.name %> (<%= movie.releaseYear %>)</h1>
  <img src="<%= movie.imageUrl %>" alt="Movie Poster" width="300" />

  <p><strong>Beschrijving:</strong> <%= movie.description %></p>
  <p><strong>Genre:</strong> <%= movie.genre %></p>
  <p><strong>Populair:</strong> <%= movie.isPopular ? '✅' : '❌' %></p>
  <p><strong>Release Datum:</strong> <%= movie.releaseDate %></p>
  <p><strong>Acteurs:</strong> <%= movie.actors.join(', ') %></p>

  <h3>🎬 Regisseur:</h3>
  <p>
    <strong>Naam:</strong> <%= movie.director.name %><br />
    <strong>Nationaliteit:</strong> <%= movie.director.nationality %><br />
    <strong>Awards:</strong> <%= movie.director.awards %><br />
    <a href="/directors/<%= movie.director.id %>">Bekijk regisseur</a>
  </p>
</body>
</html>
